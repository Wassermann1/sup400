Compiler and linker tools from VRT

Replaced make.exe for newer version that actually works

 - Reset handlers from real console dump and Nes maker rom
 - Disply init cut  - fragment of startup routine from console could be inserter ito NesMaker generated rom.
 current version initializes APU and turns on screen backlight but doesn't produce picture