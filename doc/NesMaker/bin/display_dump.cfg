MEMORY {
    RAM: start = $0000, size = $8000;
    ROM: start = $B5F0, size = $0A10, fill = yes; # оригинальный размер
}

SEGMENTS {
    CODE: load = ROM, type = ro;
    DATA: load = RAM, type = rw;
    ZEROPAGE: load = RAM, type = zp;
}